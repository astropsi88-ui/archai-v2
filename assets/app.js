const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const phrases={companion:'Хочу создать AI-компаньона, который будет рядом для жизни, дел и общей истории.',partner:'Хочу создать личного ИИ-напарника для рабочих задач, проектов и процессов.',coauthor:'Хочу создать цифрового соавтора для текстов, сценариев и регулярного контента.',project:'Хочу создать цифровую личность проекта для Telegram и сайта.',start:'Не знаю, с чего начать. Помогите выбрать подходящую цифровую личность.'};
function typeLoop(el,text){if(!el||matchMedia('(prefers-reduced-motion: reduce)').matches){if(el)el.textContent=text;return}let i=0,del=false;setInterval(()=>{el.textContent=text.slice(0,i);if(!del&&i<text.length)i++;else if(!del)del=true;else if(i>0)i--;else del=false},75)}
typeLoop($('.typing'),'Напишите Вику своими словами, кого вы хотите создать…');
$$('.tag').forEach(b=>b.addEventListener('click',()=>{const text=phrases[b.dataset.prompt]||b.textContent.trim();const input=$('#chat-input');if(input){input.value=text;input.focus()}$('#hero-chat')?.scrollIntoView({behavior:'smooth',block:'start'})}));
const toggle=$('.nav-toggle'),nav=$('#site-nav');
function closeNav(){if(!toggle||!nav)return;toggle.setAttribute('aria-expanded','false');toggle.setAttribute('aria-label','Открыть меню');nav.classList.remove('is-open');document.body.classList.remove('nav-open')}
function openNav(){if(!toggle||!nav)return;toggle.setAttribute('aria-expanded','true');toggle.setAttribute('aria-label','Закрыть меню');nav.classList.add('is-open');document.body.classList.add('nav-open');nav.querySelector('a')?.focus()}
if(toggle&&nav){toggle.addEventListener('click',()=>toggle.getAttribute('aria-expanded')==='true'?closeNav():openNav());nav.addEventListener('click',e=>{if(e.target.closest('a'))closeNav()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeNav()})}
const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;
if('IntersectionObserver'in window&&!reduce){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('revealed');io.unobserve(e.target)}}),{threshold:.18});$$('.reveal,.card,.node,.price-card,.network-node,.media-block').forEach((el,i)=>{el.classList.add('reveal');el.style.transitionDelay=`${Math.min(i%7*70,420)}ms`;io.observe(el)})}else{$$('.reveal,.card,.node,.price-card,.network-node,.media-block').forEach(el=>el.classList.add('revealed'))}
$$('.card,.price-card').forEach(el=>el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.setProperty('--x',`${e.clientX-r.left}px`);el.style.setProperty('--y',`${e.clientY-r.top}px`)}));
